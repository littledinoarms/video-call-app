<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Video Call</title>
  <script>const ROOM_ID = "<%= roomId %>"</script>
  <script src="https://unpkg.com/peerjs@1.3.1/dist/peerjs.min.js" defer></script>
  <script src="/socket.io/socket.io.js" defer></script>
  <script src="script.js" defer></script>
  <style>
    body { background: #1a1a1a; color: white; font-family: sans-serif; }
    #video-grid { display: flex; flex-wrap: wrap; gap: 10px; justify-content: center; }
    video { width: 400px; height: 300px; object-fit: cover; background: #000; border-radius: 10px; }
    .controls { position: fixed; bottom: 20px; left: 50%; transform: translateX(-50%); display: flex; gap: 20px; }
    button { padding: 12px 24px; border-radius: 30px; border: none; cursor: pointer; font-weight: bold; }
    #mute-btn { background: #ea4335; color: white; }
    #video-btn { background: #3c4043; color: white; }
  </style>
</head>
<body>
  <div id="video-grid"></div>
  <div class="controls">
    <button id="mute-btn">Mute Mic</button>
    <button id="video-btn">Stop Video</button>
  </div>
</body>
</html>
